# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository state

This repository is currently empty of actual project content. The only tracked file,
`Claudedeisgn.git`, is not a git bundle or project file — it is GitHub's boilerplate
"…or create a new repository from the command line" snippet (`git init`, `git add`,
`git commit`, `git push`, etc.) that was committed as a text file instead of being
run as shell commands.

There is no source code, README, package manifest, build system, linter, or test
suite to document yet. When a real project is scaffolded here, this file should be
rewritten to describe its actual build/lint/test commands and architecture.
